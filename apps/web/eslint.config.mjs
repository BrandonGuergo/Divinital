import { config } from "@divinital/eslint-config/next";

const webConfig = [
  // Next.js-generated, not meant to be edited or linted.
  { ignores: ["next-env.d.ts"] },
  ...config,
];

export default webConfig;
